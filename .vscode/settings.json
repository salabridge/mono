{
  "lnf.aliases": {
    "$/": "${workspaceRoot}/",
    "$lib/":"${workspaceRoot}/src/lib/",
    "$src/": "${workspaceRoot}/src/",
    "$components/": "${workspaceRoot}/src/lib/components",
    "$routes/": "${workspaceRoot}/src/routes"
  }
}
